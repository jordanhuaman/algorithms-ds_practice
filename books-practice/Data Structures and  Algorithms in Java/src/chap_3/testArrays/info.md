### General Understanding
> We could think how we can compare object, event more deeply how we can compare list of object inside.
- In general, when we build a package, we are required to implement a Object.equals(Object) method
  that override the previous one.
- In this chapter, we are building a DeelListComparison to achive the previous objective of the chapter.
- We need to complare element per element inside each object.