### Genenal understanding
- In the basic use of DoubleLL, we should now that the entide DoubleLL is not empty,because
  when we instance the class, we have the header an the next class yet, so we can acces the element inside them, although this classes contains null reference and elements
- The node class is not similar to the other node.