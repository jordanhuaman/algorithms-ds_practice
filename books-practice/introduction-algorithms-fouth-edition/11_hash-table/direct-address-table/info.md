### Direct Address Table
- This data structure is like an "Array" way, instead storing the relative position like a key, we store a different key where the person decides a custom key.
- search, insert and delete -> O(1)